name: Test Cases
on: [push]
jobs:
  dependency_install:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Setting up testing environment and install dependencies!!!"
      - run: echo "This is where we are going to install our dependencies"
  django_unittests:
    runs-on: ubuntu-latest
    needs: dependency_install
    steps:
      - run: echo "This is where we run our unittests"
  linting:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is where we run our black and js linter"
  selenium_tests:
    runs-on: ubuntu-latest
    needs: dependency_install
    steps:
      - run: echo "We run selenium tests here"
